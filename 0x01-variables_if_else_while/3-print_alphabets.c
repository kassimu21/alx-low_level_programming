#include <stdio.h>
#include <unistd.h>
/**
 * main - Entry point
 * Descrition: prints alphabet in lowercase then upercase
 * Return: Always 0 (Success)
 */
int main(void)
{
	int ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		putchar(ch);
	}
	for (ch = 'A'; ch <= 'z'; ch++)
	}
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
